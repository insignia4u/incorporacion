<div class="container">
  <div class="row">
    <div class="span12">
      <% flash.each do |key, f| %>
        <div data-type="<%= key.to_s %>" class="alert alert-<%= key.to_s %> alert-system">
          <p><%= f %></p>
        </div>
      <% end -%>
    </div>
  </div>
</div>
